start->[
    var num id;
    var letter od;
    var stupids = new Array(2);
    var efe =new Array(2)[1,2,3,4];
    var letter aja=c;
	if(cont :>0 ~ chuper:<69 )<
		throwDown(mayor);
	>eif(cont:=2)<
		throwDown(menor);
	>else<
		throwDown(sepa);
	>

    func num Recursiva (letter Zeta)[
		block(cont)->
			case 0 <
				var num con=1;
				throw(contador);
			>
			case 1<
				var num con=2;
				throw(adondeVAS);
			>
			default<
				throwDown(No);
			>
		end
	reply(Zeta)
	]

]
