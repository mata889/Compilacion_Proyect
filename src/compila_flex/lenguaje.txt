start->[
    var num contador=0;
    for(cuenta in 0...5)->
    <
        var num vamos=9;
        if(vamos :>0  contador:<69 )<
		    throwDown(mayor)
	    >eif(cont:=2)<
		    throwDown(menor);
	    >else<
		    throwDown(sepa);
	>

    >
]
