start->[
    var num id;
    var letter od;


	for(initial in 1...3)->
	<
		catch(char,lectura);
		throw(Hola);
		throwDown(HolaComOEstas);
		var num cont = 1;
	>

	func num Recursiva (letter Zeta)[
		var num cheq;
	reply(Recursiva(letter xexe))
    ]
]
