start->[
    var num id;
    if(cont :>0 ~ ejemplo:<69 )<
        throwDown(!mayor A CERO!);
    >eif(cont:=2)<
        throwDown(!menor a cero!);
    >else<
        throwDown(!imposible ese error!);
    >
    
    for(initial in 1...3)->
    <
        catch(char,lectura);
        throw(Hola);
        throwDown(olaComOEstas);
        var num cont = 1;
    >	
]