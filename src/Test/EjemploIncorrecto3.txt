start->[
    var letter aja=c;
    var arr = new Array(1)
    var arr2 = new Array(2);
    if(cont :>0 ~ ejemplo:< )<
        throwDown(mayor);
    >eif(cont:=2)<
        throwDown(menor);
    >else<
        throwDown(imposible);
    >
    
    for(initial in 1...3)->
    <
        catch(char,lectura),
        throw(Hola);
        throwDown(HolaComOEstas);
        var num cont = 1;
    >
    func num Recursiva (letter Zeta)[
        block(cont)->
            case 0 <
                var num Zeta=1;
                throw(cont);
            >
            case 1<
                var num Zeta=2;
                throw(cont);
            >
            default<
                throwDown(No);
            >
        end
    reply(Zeta)
    ]
    wle(expresion)->
    <
        var num express=1;
    >

]