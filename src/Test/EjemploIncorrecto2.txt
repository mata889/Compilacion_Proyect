start->[
	var num numGlobal;
	func void main()[
		var num varchar;
		varchar = 0;	
		var letter varExeter = 'a';
		var bool varEchelon = True;
		var num x = 5 + varchar/(5-2);

		wle (x <: 3)-><
			var letter letra = 'x';
			block (letra)->
				case 'x' <
					var num z;
				>
				case 'z' <
					var num z;
				>
				default <>
			end
			throw(x);
		>

		for (i in 0...6) ->
		<
			if ( x<:3 || x :> 15 && x :> 10 )<
				throwDown(x);
			>else<
				var letter letrita = 'c';
				throw();
			>
		>
	]
	
]