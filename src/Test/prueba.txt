start->[
    
    func num recursiva (num Zeta, num num2)[
        reply(Zeta)
    ]

    var num num1 = 0;
    var num num2 = 0;
    var num retorno = recursiva(num1, num2);
    retorno = recursiva(num1, num2);
]